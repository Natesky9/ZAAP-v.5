var get_data_structure = argument0

var get_type = data_structure_type(get_data_structure)

if get_type == ds_type_list
    {//clear list
    for (var i = 0;i < ds_list_size(get_data_structure);i += 1)
        {
        var get_value = ds_list_find_value(get_data_structure,i)
        }
    
    
    }
